import styled from "styled-components"

export const StVideoContainer = styled.div({
    width: "700px",
    margin: "20px",
    marginLeft: "130px"
})

export const StVideo = styled.video({
    width: "100%"
})

export const StVideoTitle1 = styled.div({
    fontWeight: "bold",
    fontSize: "26px",
    marginTop: "10px"
})

export const StVideoTitle2 = styled.div({
    color: "#888788"
})

export const StVideoSubTitleContainer = styled.div({
    marginTop: "15px",
    marginBottom: "15px",
    paddingBottom: "15px",
    borderBottom: "1px solid lightgray",
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
})

export const StActions = styled.div({
    display: "flex",
    flexDirection: "row",
})